# Benchmarks

Store benchmark specs and snapshots here.

Initial active harness currently lives at:
- `/data/workspace/scripts/mcp-bench/run-gog-vs-mcp-bench.sh`

Target migration:
- make this repo own benchmark fixtures + runner end-to-end.
